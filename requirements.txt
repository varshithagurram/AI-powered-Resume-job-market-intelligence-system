# Scraping
requests
beautifulsoup4
selenium

# Data handling
pandas
numpy<2   # pinned: spacy/thinc's compiled extensions require numpy 1.x

# Resume parsing
pdfplumber
python-docx
docx2txt

# NLP / matching
# Install spacy first, then run: python -m spacy download en_core_web_sm
# (don't pin the model wheel URL -- it must match whatever spacy version
# pip resolves, which can drift; the spacy downloader handles this automatically)
spacy
sentence-transformers
scikit-learn<1.5
scipy<1.13

# Database
sqlalchemy
psycopg2-binary   # PostgreSQL driver (remove if using SQLite only)

# App
streamlit

# Utilities
python-dotenv
tqdm
